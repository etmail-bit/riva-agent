# 零洩漏原則：真實店家原始資料、機敏設定絕不進版控
# 用 ** 遞迴比對，避免以後多一層子資料夾（例如備份檔）就沒被排除到
# CLAUDE.md 含個人背景/轉職規劃說明，repo 公開後不放上去，只留本機給 AI 助理看
CLAUDE.md
# .claude/ 是給 AI 助理看的工具設定/skill，跟 CLAUDE.md 同一個道理不進版控
# （settings.local.json／scheduled_tasks.lock 目前是靠全域 gitignore 排除，
# skills/ 子資料夾沒被那條規則涵蓋到，這裡明講一次比較保險，不要只靠全域設定）
.claude/
data/raw/
data/**/*.xlsx
data/**/*.csv
!data/*.example.csv
排班/
*.jpg
*.jpeg
*.png
*.pdf
.env
.env.local
.env.*.local
!.env.example
config/**/*.json
!config/*.example.json
config/**/*.yaml
!config/*.example.yaml
db/**/*.db
db/**/*.sqlite3
reports/*
# 00_報表總覽.md 是靜態導覽檔，不含真實數字，2026-07-14 使用者確認可以公開，
# 其他帶日期後綴的報告（含真實營收/成本/薪資數字）繼續排除。用 reports/* 而不是
# reports/ 排除整個目錄，是因為 git 有個規則：父目錄本身被排除的話，底下的檔案
# 沒辦法再用 !pattern 個別解除排除，只有排除「目錄底下的內容」才能這樣做。
!reports/00_報表總覽.md
*.log
__pycache__/
*.pyc
.venv/
venv/
node_modules/
.DS_Store
.playwright-mcp/
.streamlit/
