# 用 pip install -r requirements.txt 還原環境
# 注意：cryptography 特別釘住 45.0.7，因為新版 (49.0.0) 在這台機器的
# Python 3.14 + Intel Mac 組合下沒有現成的預編譯包，會觸發原始碼編譯失敗
# （缺 Rust/OpenSSL 工具鏈），所以刻意鎖在有預編譯包的版本。
cryptography==45.0.7
streamlit==1.59.0
streamlit-authenticator==0.4.2
openpyxl==3.1.5
pandas==3.0.3

# numpy/pyarrow 原本沒釘版本，2026-07-13 雲端部署時因此抓到剛發布的 pyarrow 25.0.0，
# 在 Streamlit Cloud 的 Linux + Python 3.14 環境下，圖表資料轉換觸發 C++ 層級
# segmentation fault（Python 的 try/except 攔不到，整個 process 直接死掉，
# 畫面顯示「Oh no. Error running app.」）。本機用 24.0.0 測試同一批資料完全正常，
# 兩邊都有現成的 Linux cp314 預編譯包，故直接釘住這組已驗證過的版本，避免雲端每次
# 重新部署都可能抓到還沒踩過雷的最新版。
numpy==2.5.1
pyarrow==24.0.0

python-dotenv==1.2.2
